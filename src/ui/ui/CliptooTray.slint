export component CliptooTray inherits SystemTrayIcon {
    icon: @image-url("../assets/cliptoo.svg");
    tooltip: @tr("Cliptoo — Clipboard Manager");
    title: "Cliptoo";

    callback toggle-window();
    callback quit-app();

    clicked => {
        toggle-window();
    }

    Menu {
        MenuItem {
            title: @tr("Show / Hide");
            icon: @image-url("icons/eye.svg");
            activated => {
                toggle-window();
            }
        }

        MenuSeparator { }

        MenuItem {
            title: @tr("Quit");
            icon: @image-url("icons/log-out.svg");
            activated => {
                quit-app();
            }
        }
    }
}
