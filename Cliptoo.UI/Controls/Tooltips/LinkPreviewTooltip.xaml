<UserControl
    x:Class="Cliptoo.UI.Controls.Tooltips.LinkPreviewTooltip"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Cliptoo.UI.Converters"
    xmlns:core="clr-namespace:Cliptoo.Core;assembly=Cliptoo.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:Cliptoo.UI.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="100"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <StackPanel Margin="5">
        <TextBlock
            FontStyle="Italic"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Text="Loading title..."
            Visibility="{Binding IsPageTitleLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <TextBlock
            FontSize="{Binding CurrentSettings.PreviewFontSize}"
            FontWeight="Bold"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
            Text="{Binding PageTitle}"
            TextWrapping="Wrap"
            Visibility="{Binding IsPageTitleLoading, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter={x:Static core:AppConstants.ConverterParameterInverse}}" />
        <TextBlock
            Margin="0,4,0,0"
            FontFamily="{Binding PreviewFont}"
            FontSize="{Binding CurrentSettings.PreviewFontSize}"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Text="{Binding Content}"
            TextTrimming="CharacterEllipsis" />
        <ContentControl Content="{Binding}" ContentTemplate="{StaticResource TagsTemplate}" />
    </StackPanel>
</UserControl>
